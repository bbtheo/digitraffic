% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dt_stations_detailed.R
\name{dt_stations_load_details}
\alias{dt_stations_load_details}
\title{Fetch and cache detailed metadata for all LAM/TMS stations}
\usage{
dt_stations_load_details(max_active = 10L)
}
\arguments{
\item{max_active}{Integer. Maximum number of concurrent HTTP requests.
Default is \code{10}.  Increase for faster fetching on a fast connection;
decrease to be more polite to the API.}
}
\value{
Invisibly returns the detailed stations tibble.
}
\description{
Downloads extended metadata (municipality, province, road number, …) for
every station using parallel HTTP requests and saves the result to the
user cache directory (\code{tools::R_user_dir("digitraffic", "cache")}).
}
\details{
After running this function the new filters in \code{\link[=dt_stations]{dt_stations()}} —
\code{road_number}, \code{municipality}, \code{province}, and \code{bbox} — will use the
refreshed data.  You only need to re-run this when the package warns that
the cache is out of date (i.e., new stations have been added to the network).
}
\examples{
\dontrun{
dt_stations_load_details()
}
}
