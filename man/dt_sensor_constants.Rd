% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dt_sensor_constants.R
\name{dt_sensor_constants}
\alias{dt_sensor_constants}
\title{Get sensor calibration constants for LAM/TMS stations}
\usage{
dt_sensor_constants(id = NULL)
}
\arguments{
\item{id}{\code{NULL} (default) to return constants for all stations, or a
single positive integer station \code{id} to filter to one station.}
}
\value{
A tibble with columns:
\describe{
\item{station_id}{Integer. Station id.}
\item{data_updated_time}{POSIXct (UTC). When the constants were last updated.}
\item{name}{Character. Constant name: \code{"VVAPAAS1"}, \code{"VVAPAAS2"},
\code{"MS1"}, \code{"MS2"}, or \code{"Tien_suunta"}.}
\item{value}{Double. Constant value (speed in km/h, count in veh/h,
or bearing in degrees).}
\item{valid_from}{Character. Start of validity period, \code{"MM-DD"} format.}
\item{valid_to}{Character. End of validity period, \code{"MM-DD"} format.}
}
}
\description{
Fetches station-specific calibration values: free-flow speeds
(\code{VVAPAAS1}/\code{VVAPAAS2}), maximum hourly capacity (\code{MS1}/\code{MS2}), and road
direction (\code{Tien_suunta}).  Many stations have seasonal variants with
separate summer and winter values.
}
\examples{
\dontrun{
# Constants for all stations
dt_sensor_constants()

# Constants for a single station
dt_sensor_constants(id = 23001)

# Free-flow speeds only
library(dplyr)
dt_sensor_constants() |>
  filter(grepl("^VVAPAAS", name))
}
}
